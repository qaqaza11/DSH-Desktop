# DSH Desktop 自动更新源(模仿 Oh-DSH 的 app-update.yml)
# 客户端会请求 https://api.github.com/repos/<owner>/<repo>/releases/latest,
# 与自身版本做严格 SemVer 比较; 有新版本时下载安装包并提示升级。
# 环境变量 DSH_DESKTOP_UPDATE_URL / DSH_DESKTOP_UPDATE_REPO 可以覆盖本文件。
owner: qaqaza11
repo: DSH-Desktop
