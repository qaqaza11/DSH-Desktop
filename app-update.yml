# DSH Desktop 自动更新源(模仿 Oh-DSH 的 app-update.yml)
# 发布前取消注释并填上你的 GitHub 仓库; 保持注释状态则随包分发后不生效,
# 客户端会退回到 DSH_DESKTOP_UPDATE_REPO / DSH_DESKTOP_UPDATE_URL 环境变量。
#
# owner: your-github-name
# repo: dsh-desktop
